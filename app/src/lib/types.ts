// Map view modes. "overview" is neutral; "default" shows the active score ramp;
// "coverage" and "uncertainty" retain the existing diagnostic views.
// Outlook is a separate gated boolean handled alongside these.
export type ViewMode = "overview" | "default" | "coverage" | "uncertainty";
