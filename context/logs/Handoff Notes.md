# Handoff Notes

## Immediate Next Step

Implement `TASK-068`, the deeper complete-region EDA gate. Owner QA accepted TASK-066 and TASK-067 and approved a regional two-act working hypothesis: first show a Pacific region moving in different directions, then reveal unequal evidence visibility, then release the reader into the existing explorer. Test that structure with complete distributions, cross-currents, separate condition/visibility heatmaps, pairwise diagnostics, ordering sensitivity, and centroid maps. Keep the index, public app data, scene copy, and current visual system unchanged. `TASK-064` remains in owner review and `TASK-057` remains `needs-fix`; do not resume story implementation, deployment, or submission.

## Next Build Step

The approved chain is `TASK-065` acquisition -> `TASK-066` processing -> `TASK-067` candidate atlas -> `TASK-068` regional evidence test -> `TASK-069` story selection -> `TASK-071` roadmap. `TASK-070` remains completed repository-cleanup history. Use existing context files; do not create per-task Markdown.

The fullscreen composition remains the preferred visual system. The existing premise, five evidence scenes, Nauru/Tuvalu comparison, title, and index-first narrative are provisional. JSD remains selected-place panel evidence only unless a future approved story explicitly changes that contract.

## Current Evidence Snapshot

- Indicator forensics preserve 182 trace rows and flag 11 within-indicator outliers.
- Country story labels identify 5 primary high-gap geographies: PN, NR, AS, WF, and TV.
- Spatial typologies point to Polynesia as the highest mean-gap subregion, with caveats against statistical cluster or adjacency claims.
- Outlook interpretation is display guidance for stress-test layers, not forecasting.
- Monitoring-gap priorities identify PN, NR, AS, and WF; AS and WF should be framed as reporting gaps unless independently verified.
- Evidence Fingerprint Divergence has 22 fingerprints, 231 unordered pairwise JSD rows, and 66 nearest-neighbor rows. TASK-037 wires nearest neighbors into selected-place detail only.
- Indicator forensics contain 165 score-input rows and 17 responsibility-context rows across 182 trace rows. Generated geography records now expose those roles separately through `TASK-048`.
- The app map now uses MapLibre with Natural Earth land context under centroid point features. Official/selectable boundary polygons are not joined and should not be implied in copy or design.
- The Dataviz Inspiration audit favors full-bleed map, selected-anchor, compact evidence-strip, direct-label, and evidence-bearing-motion patterns. Treat references as principle studies only.
- `TASK-049` approved the generated desktop/mobile concept frames and locked the evidence-mark direction before implementation. Codex retains evidence/claims QA, staging, and commit responsibility; commits never receive assistant co-author trailers.
- `TASK-066` is owner-accepted. It processes the five accepted candidates into 16,410 rows across 14 datasets and leaves crop yield absent. Generic JSON metadata retains SDMX disaggregation without bespoke schemas; candidate content hashes match the validated manifest; the original nine row hashes remain stable; and the serialized 22-row gap index plus 182-row trace are unchanged. No public app or story file consumes candidate data.
- `TASK-067` is owner-accepted. It records one comparison judgment per candidate and eight story hypotheses: four supported, one weak, one contradicted, and two unavailable. The figures expose different reporting clocks, within-indicator distributions/trends, named-place profiles, sparse loss reporting, and three non-selected auditions. Strongest trace: PG/WS water rises while renewable share falls; weakest: land-cover values reproduce but direction/baseline semantics remain unresolved. Direct loss remains a 39-row reporting record, not a zero-filled trend.
- `TASK-070` removed unused notebook/Kaggle/Hugging Face/report/interim scaffolding, empty analysis packages, unused artifact lanes, empty fixtures, and redundant markers. Only the explicitly justified empty figure and run-bundle lanes remain; do not recreate removed scaffolding without a concrete task.

## 2026-07-14

- Owner accepted TASK-066/TASK-067 and approved a regional two-act working direction, not final scene copy. Added TASK-068 for deeper regional distributions, cross-currents, separate condition/visibility matrices, relationship/dependency review, clustering sensitivity, centroid maps, and a deterministic run bundle. Final story selection moves to TASK-069; roadmap work moves to TASK-071 while completed TASK-070 retains its cleanup identity. No standalone task Markdown, app change, new score, or publication claim was added.

## 2026-07-13

- 2026-07-13: `TASK-067` Builder pass moved to `in-review`. The EDA runner now generates three candidate evidence tables, a five-figure Matplotlib research atlas, and one contact sheet with three provisional auditions. Full-size inspection passed for coverage/alignment, distributions, small-multiple trends, named-place contrasts, direct-loss reporting visibility, and the audition board. Raw-to-processed endpoints reproduced the PG/WS cross-current and the deliberately withheld land-cover signal; `USD_MILLIONS` conversion and missing-loss semantics are explicit. No composite, final story, public app field, or scene changed; owner QA retained acceptance. The then-planned selection gate was later renumbered to TASK-069 after TASK-068 became the deeper regional EDA.
- 2026-07-13: `TASK-066` Builder pass moved to `in-review`. The accepted candidates now contribute 2,403 rows to a deterministic 16,410-row/14-dataset processed table; crop yield remains excluded. Focused TDD locks selection, generic SDMX metadata preservation, metadata-aware candidate hashes, legacy hash stability, and strict exclusion from gap-index outputs. Two-row manual traces per candidate and manifest checks passed; serialized comparison proved the 22-row index and 182-row trace unchanged. Owner QA retains acceptance.
- 2026-07-13: Owner approved the refined exploration contract before TASK-066 begins. TASK-067 now requires a five-figure static visual research atlas followed by one contact sheet with three rough evidence-board story auditions. Matplotlib is the default future renderer; Plotly or new GIS dependencies require a demonstrated analytical need, and centroid geometry remains the spatial limit. The then-planned selection gate was later renumbered to TASK-069. No app, score, processed data, or scene implementation changed in this planning pass.

## 2026-07-12

- 2026-07-12: QA completed `TASK-070`. The audit removed 15 unused directories and 25 tracked scaffold/marker files while preserving every imported package, runtime file, data product, generated artifact, and the two empty lanes with concrete current references. Full Python passed 75/75 and repository gates passed; next work remains `TASK-066`.
- 2026-07-12: Independent Checker approved `TASK-065` at `9f7f3da` with no remaining findings. Final evidence is 15/15 primary plus 1/1 supplementary live fetches, 15/15 profiles, 75/75 Python tests, and a value-equivalent nine-source processed rebuild with 14,007 rows across 22 geographies. The acquisition gate accepts five processing-disabled candidates for `TASK-066`, rejects aggregate crop yield, preserves exact fallback/hash/coverage provenance, and leaves tracked processed, score, app, and story artifacts unchanged. QA moved the task `in-review -> done`.
- 2026-07-12: Final `TASK-065` cache-integrity repair returned to `in-review`. Live-cache writes use one exact UTF-8 payload for disk/count/hash, invalid UTF-8 and read failures return cache errors, and a present manifest is validated before missing-file fallback so neither profiler nor processed loader can create an unmanifested replacement. Focused passed 18/18, live fetch 15/15 plus supplementary 1/1, profile 15/15, full Python 75/75, and the nine-source 14,007-row value-equivalence gate passed; no tracked downstream artifact changed.
- 2026-07-12: Checker returned `TASK-065` for five gaps; the repair is back in `in-review`. Six processing-disabled flags and a central selector prove 15 profile inputs versus nine current processing inputs. Cache status/hash validation now blocks stale files, fetch/contracts retain original 422 plus effective stable endpoint/note/hash, and profiles separate returned-row blanks from structurally absent geography-years. The disaggregated crop pull is reproducible through one optional CLI flag and a separate 20,725-row manifest entry. Live fetch passed 15/15 plus 1/1, profiles passed 15/15, focused passed 14/14, full Python passed 71/71, and a nine-source rebuild was value-equivalent across 14,007 rows/22 geographies; documented stable response hashes differ from the older tracked provenance, so tracked processed files remain untouched until TASK-066.
- 2026-07-12: `TASK-065` Builder pass moved to `in-review`. A focused TDD pass added six candidate configurations, semantic/licence/grain/decision contract fields, cache-first profiling, stale-cache manifest protection, and the documented Pacific stable-API fallback for v2 HTTP 422. Live verification required the full agency/flow/version reference because short flow IDs returned 403 for four SDG sources; the final ignored manifest records 15/15 successes and SHA-256 hashes. Profiles reproduce the candidate snapshots: population 792/22/1990–2025, renewable 461/20/2000–2023, water 430/19/2000–2022, crop 900/15/1961–2024, loss 39/12/2007–2020, and land cover 681/22/1992–2022. Five are accepted only for TASK-066; crop is rejected after the supplementary 20,725-row item source exposed 78 crops/two production types. Full Python passed 66/66 and standard gates passed; independent QA owns `done`.
- 2026-07-12: Owner approved a data-first narrative reset. Added `TASK-065`–`TASK-069` for targeted official-data acquisition, processing, comparability/story EDA, scientific/owner story selection, and roadmap rewrite. Candidate datasets are population growth, renewable-energy share, safely managed drinking water, crop yield, direct disaster economic loss, and climate-altering land cover. No candidate is accepted, score-wired, app-wired, or story-wired yet; the current fullscreen visual system remains the baseline while its narrative is provisional. No standalone task Markdown was created.
- 2026-07-12: `TASK-064` QA Maker advanced to owner-gated `in-review`. The first browser matrix found progress controls mounted about 773px above active evidence scenes and therefore not normally clickable. A focused RED/GREEN repair wraps the existing toolbar and `SceneProgress` in one sticky chrome region, gives progress controls 44px targets, and aligns rank sticky clearance to measured 120px desktop / 132px mobile chrome. Normal progress clicks passed at 1440×900, 390×844, and 844×390; the refreshed 25-PNG matrix, exact automated gate (60 Python, 44 frontend, 1,024,043-byte JS, 94,829-byte CSS), source/evidence/accessibility checks, URL/history/handoff/rapid-key checks, and Semgrep passed. Owner acceptance is still required; TASK-057 remains `needs-fix`.
- 2026-07-12: Root returned `TASK-063` through `in-review -> needs-fix -> in-progress` again after focused `main.story-scrolly` input showed immediate scroll alone was insufficient: observer-lagged React `index` still caused End → Home → PageDown to settle on scene 3. Builder rejected and deleted a nearest-section hypothesis after it failed the exact harness. One pending requested-index ref now remains authoritative across keyboard/progress input and clears only when pointer/wheel/touch returns ownership to manual scroll; intermediate observer callbacks cannot replace it. The exact focused-main sequence passed twice at 1280×800 and twice at 390×844 with scene 1, top 58px, canonical URL, retained main focus, and zero overflow. Slower focused-main keys and copied URL/handoff/Explore/Back passed. TASK-063 returned only to `in-review`; no timer/controller/observer/store/dependency was added.
- 2026-07-12: Root returned `TASK-063` through `in-review -> needs-fix -> in-progress` after the exact 1280×800 End → 40ms → Home → 40ms → PageDown sequence settled on scene 3 instead of scene 1. Builder independently reproduced nondeterministic wrong scene-4 settlement twice. Programmatic story jumps now use immediate native scroll, leaving CSS evidence transitions to carry visual continuity. The exact sequence passed twice at 1280×800 and twice at 390×844, always ending scene 1 with its canonical URL and zero overflow; slower Arrow/Page/Home/End, copied scene 5, handoff reset, Explore, and Back passed. Focused 3/3, full frontend 42/42, build/budget at 1,023,858-byte JS and 94,804-byte CSS, and Semgrep passed. TASK-063 returned only to `in-review`; no timer/controller/observer/dependency was added.
- 2026-07-12: `TASK-063` Builder pass moved to `in-review`. Stable geography codes now span map marks, comparison portraits, and rank rows. The existing observer also observes the separate map-immersive handoff; handoff and Explore restore gap/default/no-selection/outlook-off state, reverse scrolling/Back reapply the canonical scene, and native CSS supplies reduced-motion-safe text/evidence/chamber transitions without a new scene, observer, controller, dependency, or animation engine. Focused RED/GREEN passed 6/6, full frontend 42/42, build/budget at 1,023,974-byte JS and 94,804-byte CSS, and Browser MCP desktop/mobile identity, copied URL, rapid navigation, handoff, Explore, Back, controls, and overflow checks passed. Root retains reduced-motion emulation, full viewport, and independent Checker acceptance.
- 2026-07-12: `TASK-062` Builder mobile-landscape fix returned to `in-review`. The existing 71px declaration now follows the general max-880px toolbar breakpoint. Browser MCP measured toolbar/sticky at 71/71px with zero overlap and horizontal overflow at 390×844 and 844×390; 1024×768 retained top 56px with active rank bands. Focused 6/6, full frontend 42/42, build/budget at 1,023,437-byte JS and 94,025-byte CSS, status, secret, and whitespace gates passed. Root retains full browser and Checker reacceptance.
- 2026-07-12: Checker returned `TASK-062` through `in-review -> needs-fix -> in-progress`: at 844×390 the 71px mobile toolbar still overlaid the rank sticky head because the first fix was portrait-only, while the 44px mobile buttons apply to the complete max-880px breakpoint. Builder is moving the same declaration to that existing general media block and will verify 390×844, 844×390, and 1024×768 before returning to review.
- 2026-07-12: `TASK-062` Builder fix returned to `in-review`. One portrait breakpoint declaration moves the sticky rank heading to the measured 71px toolbar edge. Builder browser verification at 390×844 showed toolbar bottom 71px, sticky top 71px, zero overlap and horizontal overflow; desktop retained top 56px. Focused 6/6, full frontend 42/42, build/budget at 1,023,437-byte JS and 94,025-byte CSS, status, secret, and whitespace gates passed. Root retains full browser and separate Checker acceptance.
- 2026-07-12: Root browser QA returned `TASK-062` through `in-review -> needs-fix -> in-progress`: at 390×844 the rank sticky head used desktop's 56px offset beneath the approximately 70px mobile story toolbar, obscuring its 56–71px intro. Source tracing identified the breakpoint offset mismatch; Builder is applying only a portrait-scoped correction before returning to review.
- 2026-07-12: `TASK-062` Builder pass moved to `in-review`. The miniature rank SVG is now a semantic unordered interval field with alphabetical full names, exact 1/8/15/22 ticks, one 1–22 scale, no midpoint mark, stable geography identity, MH 4–19 highlighting, native page flow, a sticky portrait title/axis, and static reduced motion. Focused RED/GREEN evidence passed 6/6; full frontend passed 42/42; build/budget passed at 1,023,437-byte JS and 93,985-byte CSS; generated-data, status, secret, and whitespace gates passed. Root retains the target viewport and separate Checker gates.
- 2026-07-12: Root browser QA returned `TASK-061` through `in-review -> needs-fix -> in-progress` after a comparison opened at 1440×900, changed in the same tab to 390×844, and reloaded into scene 3. The late `App` effect allowed browser restoration to race startup hydration. The minimal fix now invokes the existing pure scroll-restoration helper in `main.tsx` before React root creation/render and removes the effect call; no timeout, controller, observer, dependency, or helper change was added. Focused tests passed 4/4, full frontend passed 41/41, build/budget passed at 1,023,600-byte JS and 94,665-byte CSS, and status/secret/whitespace gates passed. `TASK-061` returned only to `in-review`; root retains the exact browser reproduction and remaining visual/Checker gates.
- 2026-07-12: `TASK-061` Builder pass moved to `in-review`. Scene 4 now exposes one comparison-stage figure and stable NR/TV portrait identity, removes the unexplained Nauru-only selection bloom, aligns header/stat/caveat tracks at stage scale, retains two columns on desktop/landscape, and stacks consecutive full-width portraits only in portrait orientation. Focused RED/GREEN evidence covers the missing stage contract and selection bloom; full tests/build/budget pass. Root retains the seven-viewport browser matrix and separate Checker review.
- 2026-07-12: `TASK-060` Builder pass moved to `in-review`. The existing `StoryScrolly`, sticky map, observer, URL state, and figure routing now form a fullscreen shell with map-immersive and figure-takeover ownership; premise top/progress chrome is absent from the DOM, interactive controls retain their own keys, observer threshold zero is included, and native focus/reduced-motion rules remain visible and immediate. Root later reproduced an existing-tab landscape-to-portrait reload failure: automatic browser restoration replayed the stale landscape offset after copied-scene scrolling, placing the missingness scene at the observer line and rewriting the URL. A focused RED/GREEN test now assigns scroll restoration to the existing hydration flow with `history.scrollRestoration = "manual"`; focused tests pass 9/9, full frontend passes 41/41, and build/budget/status/secret/whitespace gates pass. Root browser re-QA remains required.
- 2026-07-12: `TASK-059` Builder pass moved to `in-review`. The app contract now exposes six stable guided IDs with `what-this-atlas-is-asking` first, map/figure stage ownership, a premise-aware eyebrow, premise URL fallback, and comparison focus tied to stable visual meaning rather than scene index. Focused tests passed 7/7 after an observed RED gate; full frontend tests passed 37/37; build and source/claims audit passed with the known chunk warning. A separate Checker must review before `TASK-059` can become done.
- 2026-07-12: Owner approved `TASK-058` and rejected per-task Markdown clutter. Consolidated the fullscreen-stage contract into `context/ARTISTIC_REDESIGN_BRIEF.md`, removed the one-off TASK-058 design file, and organized TASK-059–064 under one batch implementation plan. `TASK-057` remains `needs-fix`; implementation has not started.

## 2026-07-11

- Owner visual QA moved `TASK-057` from `in-review` to `needs-fix`: the first story view does not explain the atlas premise clearly enough, and the fixed 30rem/28rem rail plus 330px rank figure makes scenes 4 and 5 too small. Cataloged three new composition boards under `artifacts/design/task-058/` and wrote the fullscreen-stage synthesis and `TASK-059`–`TASK-064` batch. No app code, data, scoring, or generated runtime artifacts changed.

- Independent browser QA accepted `TASK-056`: desktop 1200x734, mobile 390x844, Explore handoff, all five scenes, selected NR/TV/FJ/AS/WF/MH panels, Methods & sources, and reduced-motion behavior passed with 22 marks, no overflow, expected visual attributes, 56px/44px mobile controls, and the panel-only similarity caveat. `TASK-056` is done; proceed to `TASK-057` for URL state, bundle-budget, and final readiness QA.
- `TASK-057` URL/history and hydration follow-ups passed their automated gates (60 Python tests, 34 frontend tests, contracts/artifacts/statuses/secrets, build, 1,022,289-byte JS and 93,895-byte CSS budget, whitespace), and Browser-use verified copied-scene restoration and history behavior. Owner visual QA then failed the opening clarity and scene-4/5 scale, so the task is `needs-fix`; no deployment or submission is claimed.
